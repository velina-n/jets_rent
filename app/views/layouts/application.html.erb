<!DOCTYPE html>
<html>
  <head>
    <title>JetsRent</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "https://cdn.jsdelivr.net/npm/flatpickr/dist/themes/material_blue.css", media: "all" %>
    <%= javascript_importmap_tags %>
   </head>

  </head>

  <body>
    <%= render 'shared/navbar' %>
    <%= yield %> <!-- Contenu de chaque page -->
    <%= render "layouts/footer" %> <!-- Inclure le footer -->
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </body>
</html>
